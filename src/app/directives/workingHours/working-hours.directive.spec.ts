import { WorkingHoursDirective } from './working-hours.directive';

describe('WorkingHoursDirective', () => {
  it('should create an instance', () => {
    const directive = new WorkingHoursDirective();
    expect(directive).toBeTruthy();
  });
});
